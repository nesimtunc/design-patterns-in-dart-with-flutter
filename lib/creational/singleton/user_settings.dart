class UserSettings {
  // you can give any name, ex: _internal, privateConstructor etc.
  static final UserSettings instance = UserSettings._internal();
  UserSettings._internal();

  bool darkMode = false;
}
